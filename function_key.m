% big_phi_comp_fb = compute big_phi on a subset - takes the BRs and FRs

% binary_net = this is the main script which creates a network and
%       computes over it

% comp_pers = this computes the forward and backward probabilities of every
%       subset conditioned on every state of every other subset